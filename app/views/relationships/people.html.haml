%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @followings.each do |followed_user|
        %tr
          %td.pull-left
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(followed_user.email)}?s=40")
            = link_to followed_user.fullname, user_path(followed_user)
          %td.extra-padding= followed_user.queue_items.count
          %td.extra-padding= followed_user.followers_total
          %td.extra-padding 
            = link_to followed_user, method: :delete do
              %i.glyphicon.glyphicon-remove