%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @relationships.each do |relationship|
        %tr
          %td.pull-left
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(relationship.followed.email)}?s=40")
            = link_to relationship.followed.fullname, relationship.followed
          %td.extra-padding= relationship.followed.queue_items.count
          %td.extra-padding= relationship.followed.followers_total
          %td.extra-padding 
            = link_to relationship, method: :delete do
              %i.glyphicon.glyphicon-remove